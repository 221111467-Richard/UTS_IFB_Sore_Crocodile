apiVersion: v1
kind: Service
metadata:
  name: uas-sentimen-service
spec:
  type: NodePort
  selector:
    app: uas-sentimen
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007